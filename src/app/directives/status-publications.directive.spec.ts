import { StatusPublicationsDirective } from './status-publications.directive';

describe('StatusPublicationsDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusPublicationsDirective();

    expect(directive).toBeTruthy();
  });
});
